import { User } from 'lucide-react';

export default function IconUser({ size = 24, color = 'currentColor', className = '' }) {
  return <User size={size} color={color} className={className} />;
}
